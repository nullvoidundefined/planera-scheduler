/**
 * Route segments and absolute paths for the portfolio site, shared by the router
 * table and every in-app link so a route rename touches one module, not three.
 */
export const ROUTE_ARCHITECTURE_PATH = "/architecture";

export const ROUTE_ARCHITECTURE_SEGMENT = "architecture";

export const ROUTE_DEMO_PATH = "/demo";

export const ROUTE_DEMO_SEGMENT = "demo";
