/**
 * Default values for calendar configuration: the standard Mon-Fri work week
 * and the default index-zero date used by the CPM scheduling engine.
 */
export const DEFAULT_EPOCH = "2026-01-01";
export const DEFAULT_WORK_WEEK: readonly number[] = [1, 2, 3, 4, 5];
export const MILLISECONDS_PER_DAY = 86_400_000;
